# Lectern 0.5.1 发布验证

日期：2026-09-07。

- `pnpm test`：102 项通过。
- `pnpm test:release`：29 项通过，含无签名更新契约、下载与安装确认、破损文件拒绝、安装前重复校验、上传顺序和双平台稳定清单。
- `pnpm typecheck`：通过。
- macOS：实际打包应用及最终 ZIP 解压应用分别执行隔离用户目录测试，验证启动、平台桥、含空格项目路径、会话保存、文件读写、原生 PTY/Git、账户菜单下载、取消安装和重启数据恢复。
- macOS：`codesign --verify --deep --strict` 通过；仅 ad-hoc 签名，不是 Developer ID 签名或公证。
- Windows：使用同一份生产前端构建，重新安装并裁剪 Windows x64 原生依赖，生成 NSIS 和 ZIP；不能将交叉构建或模拟安装行为测试视作 Windows 真机验收。

构建在独立临时目录完成，不使用开发服务缓存，不携带开发 `.env`。发布检查验证包内版本、平台原生依赖、解包布局、私有数据排除和所有发行文件的哈希。

发布后执行 `node scripts/verify-public-release.mjs`，回读稳定清单及整个公网安装包，校验与本地文件一致。此检查需要先完成两端包上传。

尚未验证：Windows 原生安装/跨版本升级/卸载，macOS 用户手动替换全过程，真实系统安全拦截交互。0.5.0 需手动安装 0.5.1 一次。以上限制不因此次发布而标记为已完成。
